# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: "Montville Rising | Visibility Brigade"
email: "montvillerising@gmail.com"
description: >-
  Montville Rising, in partnership with the local chapter of the
  Visibility Brigade, is a local community activist group. Find our
  mission, upcoming events, and how to get involved.
baseurl: "/montville-rising" # repo name — required since this isn't a crussell96.github.io repo
url: "https://crussell96.github.io" # switch to "https://www.montville-rising.org" (and clear baseurl) once the custom domain is ready
instagram_username: vbmontvillerising

# Build settings
theme: minima
plugins:
  - jekyll-feed

header_pages:
  - about.markdown
  - resources.markdown
  - events.markdown
  - get-involved.markdown

# Used by assets/js/upcoming-events.js on the home page to pull the next 3
# events straight from Google Calendar. See docs/events.markdown for how the
# calendar itself is set up.
calendar_id: "montvillerising@gmail.com"
calendar_api_key: "AIzaSyBiXkzt4KKnJVlMtFLJj0jCAACOLadPyIM"

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
